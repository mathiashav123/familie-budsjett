/**
 * Familiebudsjett – Supabase config (FREE tier, public anon key only).
 *
 * Wake-up: paste Project URL + anon public key from
 * Supabase → Project Settings → API. Never put service_role here.
 *
 * Auth is username + password (no e-mail). See README + supabase-schema.sql.
 */
window.FAMILIE_BUDGET_SUPABASE = {
  url: "",
  anonKey: ""
};
